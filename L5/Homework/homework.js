// Do not change any of the function names

//Example 1:
function getBiggest(x, y) {
  return x >= y;
}

function greeting(language) {
  console.log('Guten Tag!, ' + German);
  console.log('Ni Hao!, ' + Mandarin);
  console.log('Hola!, ' + Spanish);
  return Hello
}

function isTenOrFive(num) {
  returnIfNum(10 && 5);
  return false;
}

function isInRange(num) {
  rif (num < 50 && num > 20);
  return false;
}

function isInteger(num) {
  Math.floor(num)
  (num > 0.8 ); // false
  (num > 1); // true
  (num > -10); //true
  return false
}

function fizzBuzz(num) {
  if (num % 3); // return fizz
  if (num % 5); // return buzz
  if (num % 3 && num % 5); // return fizzbuzz
  return num
}


function isPrime(num) {
  for (let i =num    ; i <10            ; i++          ){
  }
    return false
 }
 

// Do not modify code below this line.
// --------------------------------

module.exports = {
  getBiggest,
  greeting,
  isTenOrFive,
  isInRange,
  isInteger,
  fizzBuzz,
  isPrime,
};